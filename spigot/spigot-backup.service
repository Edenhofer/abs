[Unit]
Description=Spigot World Backup
After=local-fs.target

[Service]
Type=forking
EnvironmentFile=/etc/conf.d/spigot
ExecStart=/usr/bin/spigot backup
USER=$MC_USER
GROUP=$MC_USER

[Install]
WantedBy=multi-user.target
